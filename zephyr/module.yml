name: zmk-keyboard-hakanserce
build:
  # Folder containing a "CMakeLists.txt" (. = root)
  depends:

  settings:
    # Folder containing a "boards" folder (. = root)
    board_root: config
    # Folder containing a "dts" folder (. = root)
    dts_root: .
    # Folder containing a "snippets" folder (. = root)
    snippet_root:  .